<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.safiraaini.safiraaini_1202154315_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuUtamaActivity" />
        <activity android:name=".MenuTakeAwayActivity" />
        <activity android:name=".MenuDineInActivity" />
        <activity android:name=".DaftarMenuActivity" />
        <activity android:name=".DaftarMenuAdapter" />
        <activity android:name=".DetailMenu"></activity>
    </application>

</manifest>